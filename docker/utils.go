package docker

import "github.com/docker/docker/api/types"

func imageListContains(summaries []types.ImageSummary, imageName string) bool {
	//for _, imgSummary := range summaries {
	//	if imgSummary.ID
	//}
	return false
}